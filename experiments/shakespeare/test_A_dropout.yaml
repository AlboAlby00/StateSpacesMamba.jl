# total params: 1987139
# model params
model_name: mamba
dataset: shakespeare

embed_dim: 128
N: 16
n_layers: 5
dropout: 0.1
ssm_dropout: 0.1
# other hyperparameters
data_to_use_percent: 1.0
train_batch_size: 64
validation_batch_size: 64
seq_len: 256
num_epochs: 100
initial_lr: 1e-3
init_fin_lr_ratio: 0.1

save_csv: true
save_model: true

use_A_dropout: [true, false]
use_cuda_scan: true

num_iterations: 2
