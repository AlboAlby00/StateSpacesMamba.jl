# total params: 1987139
# model params
model_name: mamba
dataset: mnist

embed_dim: 16
N: 2
n_layers: 3
dropout: 0.0
ssm_dropout: 0.0

train_batch_size: 4
validation_batch_size: 4

num_epochs: 100
initial_lr: 1e-4
init_fin_lr_ratio: 0.1

save_csv: false
save_model: false

use_A_dropout: false
use_cuda_scan: true

num_iterations: 1

input_dim: 28