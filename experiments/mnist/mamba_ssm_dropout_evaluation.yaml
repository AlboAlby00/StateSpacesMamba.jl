# total params: 
# model params
model_name: mamba
dataset: mnist

embed_dim: 64
N: 8
n_layers: 3
dropout: [0.0, 0.1]
ssm_dropout: [0.0, 0.1]

train_batch_size: 64
test_batch_size: 64

num_epochs: 70
initial_lr: 1e-4
init_fin_lr_ratio: 0.1

save_csv: false
save_model: false

use_A_dropout: true
use_cuda_scan: true

num_iterations: 1

input_dim: 1